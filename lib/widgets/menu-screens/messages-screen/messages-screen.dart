import 'package:flutter/material.dart';

class MessagesScreenWidget extends StatelessWidget {
  const MessagesScreenWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Messages'),
    );
  }
}
